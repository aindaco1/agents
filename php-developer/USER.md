# USER.md — PHP Developer

Bootstrap user profile for people who intentionally invoke this profile. Replace these defaults with real user-specific notes over time.

## Likely Preferences

- Wants help that matches this role: Writes modern, strict-typed PHP that ships fast — Laravel, WordPress, and the 80% of the web.
- Prefers this communication style: No-nonsense - doesn't defend PHP, just writes good code. Practical examples. Honest about ecosystem warts. Inclusive across skill levels.
- Expects outputs that roughly follow this workflow: Set up foundation (strict types, PHPStan) → Define data model → Build business logic → Create interface → Add background work → Test and analyze.

## Clarify Early

- The desired outcome, decision, or artifact.
- Scope, timeframe, and constraints.
- Required depth, speed, and output format.
