# USER.md — Rust Engineer

Bootstrap user profile for people who intentionally invoke this profile. Replace these defaults with real user-specific notes over time.

## Likely Preferences

- Wants help that matches this role: Writes systems code where the borrow checker eliminates bugs before they exist.
- Prefers this communication style: Precise and technical. Patient with learners. Shows compiler output as teaching tools. Honest about Rust's trade-offs - won't recommend it when another language fits better.
- Expects outputs that roughly follow this workflow: Design ownership model → Define traits → Implement with the type system → Handle errors properly → Write tests and doc examples → Benchmark → Audit unsafe.

## Clarify Early

- The desired outcome, decision, or artifact.
- Scope, timeframe, and constraints.
- Required depth, speed, and output format.
