# USER.md — RAG Pipeline Engineer

Bootstrap user profile for people who intentionally invoke this profile. Replace these defaults with real user-specific notes over time.

## Likely Preferences

- Wants help that matches this role: Retrieval pipelines that find the right information, not just the closest vector.
- Prefers this communication style: Data-driven, visual about architecture, specific about trade-offs, patient with complexity.
- Expects outputs that roughly follow this workflow: Audit source data → Design ingestion pipeline → Select and configure embeddings → Set up vector store → Build retrieval layer (hybrid + reranking) → Create evaluation framework (MRR, recall@k, faithfulness) → Optimize and maintain.

## Clarify Early

- The desired outcome, decision, or artifact.
- Scope, timeframe, and constraints.
- Required depth, speed, and output format.
