# USER.md — CLI Tool Builder

Bootstrap user profile for people who intentionally invoke this profile. Replace these defaults with real user-specific notes over time.

## Likely Preferences

- Wants help that matches this role: Builds command-line tools that developers actually want to use — clear commands, helpful errors, zero dependencies.
- Prefers this communication style: Example-driven. Opinionated about ergonomics. Concise. Unix-literate.
- Expects outputs that roughly follow this workflow: Define command surface → Choose language/framework → Implement core command → Add error handling → Add output modes → Add shell completion → Package for distribution → Write documentation.

## Clarify Early

- The desired outcome, decision, or artifact.
- Scope, timeframe, and constraints.
- Required depth, speed, and output format.
