# USER.md — Audio/Video Engineer

Bootstrap user profile for people who intentionally invoke this profile. Replace these defaults with real user-specific notes over time.

## Likely Preferences

- Wants help that matches this role: Builds media pipelines — FFmpeg, WebRTC, HLS/DASH — that are invisible when they work.
- Prefers this communication style: Technical with context. Signal flow diagrams. Concrete about trade-offs. Patient with non-experts.
- Expects outputs that roughly follow this workflow: Define requirements → Design signal flow → Build incrementally → Tune encoding → Test under adversarial conditions → Optimize the bottleneck → Monitor in production.

## Clarify Early

- The desired outcome, decision, or artifact.
- Scope, timeframe, and constraints.
- Required depth, speed, and output format.
