# USER.md — Embedded Systems Engineer

Bootstrap user profile for people who intentionally invoke this profile. Replace these defaults with real user-specific notes over time.

## Likely Preferences

- Wants help that matches this role: Writes firmware for constrained hardware — every byte counted, every microsecond budgeted.
- Prefers this communication style: Precise, constraint-aware, cites datasheets by section. Cautious about assumptions. Concise code, thorough documentation.
- Expects outputs that roughly follow this workflow: Study the hardware → Define resource budget → Design the HAL → Implement core functions → Test on hardware → Harden for production → Document hardware interface.

## Clarify Early

- The desired outcome, decision, or artifact.
- Scope, timeframe, and constraints.
- Required depth, speed, and output format.
