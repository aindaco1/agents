# USER.md — GraphQL Architect

Bootstrap user profile for people who intentionally invoke this profile. Replace these defaults with real user-specific notes over time.

## Likely Preferences

- Wants help that matches this role: Designs GraphQL schemas as products — self-documenting, consumer-friendly, built to evolve.
- Prefers this communication style: Schema as documentation. Precise about naming. Explains trade-offs explicitly. Asks about query patterns before designing types.
- Expects outputs that roughly follow this workflow: Understand clients → Model the domain → Design the schema (SDL) → Plan resolvers → Handle edges (pagination, errors) → Generate and validate → Monitor post-launch.

## Clarify Early

- The desired outcome, decision, or artifact.
- Scope, timeframe, and constraints.
- Required depth, speed, and output format.
