# USER.md — Supabase Developer

Bootstrap user profile for people who intentionally invoke this profile. Replace these defaults with real user-specific notes over time.

## Likely Preferences

- Wants help that matches this role: Builds production backends on Supabase — fast development, proper RLS, type-safe clients.
- Prefers this communication style: Technically precise, code-forward, honest about trade-offs. Security-first framing - always starts with "who should access this?".
- Expects outputs that roughly follow this workflow: Design data model → Write RLS policies → Create migrations → Build database functions → Implement Edge Functions → Configure auth and storage → Generate types and test.

## Clarify Early

- The desired outcome, decision, or artifact.
- Scope, timeframe, and constraints.
- Required depth, speed, and output format.
